# Калькулятор клинингового сервиса

Полнофункциональный калькулятор с базой данных, кэшированием и API.

## Что реализовано

### ✅ База данных (SQLite через Prisma)
- **CleaningCalculation** - хранит все расчеты
- **CleaningCache** - кэш данных с TTL
- Автоматическая очистка устаревших записей

### ✅ API Endpoints
- `POST /api/cleaning/calculations` - Сохранить расчет
- `GET /api/cleaning/calculations` - История расчетов
- `GET /api/cleaning/calculations/:id` - Конкретный расчет
- `DELETE /api/cleaning/calculations/:id` - Удалить расчет
- `GET /api/cleaning/statistics` - Статистика
- `GET /api/cleaning/cache/:key` - Получить из кэша
- `POST /api/cleaning/cache` - Сохранить в кэш

### ✅ Фронтенд функции
- Расчет стоимости в реальном времени
- Сохранение расчетов в БД
- История расчетов с возможностью загрузки
- Уведомления о сохранении

## Настройка

### 1. Установите зависимости сервера
```bash
cd server
npm install
```

### 2. Примените миграции БД
```bash
npx prisma migrate dev --name add_cleaning_models
npx prisma generate
```

### 3. Запустите сервер
```bash
npm run dev
```

Сервер запустится на http://localhost:4000

### 4. Запустите фронтенд
В другом терминале:
```bash
cd web
npm install
npm run dev
```

Фронтенд запустится на http://localhost:5173

## Где хранятся данные

### База данных
- **Файл**: `server/prisma/dev.db` (SQLite)
- **Просмотр**: `npx prisma studio` (откроется на http://localhost:5555)

### Кэш
- **In-memory кэш**: Быстрый доступ к часто используемым данным
- **БД кэш**: Долгосрочное хранение с автоматической очисткой

## Использование

1. Откройте калькулятор: http://localhost:5173/cleaning-calculator
2. Введите параметры уборки
3. Нажмите "💾 Сохранить расчет" для сохранения в БД
4. Нажмите "История" для просмотра сохраненных расчетов
5. Кликните на расчет из истории, чтобы загрузить его параметры

## Структура данных

### Расчет сохраняет:
- Тип услуги
- Площадь, комнаты, санузлы
- Частота уборки
- Дополнительные услуги
- Все стоимости (базовая, окна, ковры, скидки, итоговая)
- Временные метки

## Для продакшена

1. Замените SQLite на PostgreSQL в `schema.prisma`
2. Обновите `DATABASE_URL` в `.env`
3. Примените миграции: `npx prisma migrate deploy`
4. Настройте переменные окружения для кэша (Redis опционально)


